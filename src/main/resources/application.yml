spring:
    test-a:
      datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://localhost:3306/servermonitor?serverTimezone=UTC&characterEncoding=UTF-8
        username: root
        password: admin
    test-b:
      datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://localhost:3306/testmonitor?serverTimezone=UTC&characterEncoding=UTF-8
        username: root
        password: admin
#mybatis:
#  mapper-locations: classpath:mapper/**/*.xml

